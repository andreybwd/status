/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {ExaminationTime} from './examination-time.model';

describe('ExaminationTime', () => {
  it('should create an instance', () => {
    expect(new ExaminationTime()).toBeTruthy();
  });
});
